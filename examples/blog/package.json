{
  "name": "@examples/blog",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "pnpm run types && root dev",
    "build": "rm -rf dist/ && root build",
    "preview": "pnpm run build && root preview",
    "start": "root start",
    "test": "pnpm run build",
    "types": "root-cms generate-types"
  },
  "dependencies": {
    "@blinkk/root": "workspace:*",
    "@blinkk/root-cms": "workspace:*",
    "preact": "^10.10.6",
    "preact-render-to-string": "^5.2.3",
    "vite": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.18"
  }
}
