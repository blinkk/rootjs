// This bundle contains only type-level code, which is stripped during
// compilation, leaving a 0-byte chunk.
export interface EmptyBundleProps {
  name: string;
}

export type EmptyBundleId = string;
